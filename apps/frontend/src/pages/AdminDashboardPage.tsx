import { AdminControlTowerPage } from '../features/admin/pages/AdminControlTowerPage';

// /admin/dashboard is the admin landing — it renders the Control Tower section
// of the admin console (sidebar shell + Control Tower content).
export function AdminDashboardPage() {
  return <AdminControlTowerPage />;
}
